# cs625_simulation

Gazebo-only fixture content. `minimal_occlusion.sdf` is a static scene used to validate the sensor path before attaching the camera to the official CS625 model. It is not a robot-motion or NBV validation result.

