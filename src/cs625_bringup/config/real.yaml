/**:
  ros__parameters:
    use_sim_time: false
    # Deliberately empty until the PS800E1/Percipio driver and its Humble
    # source topics are verified on the real system.
    input_color_topic: ""
    input_depth_topic: ""
    input_camera_info_topic: ""
    input_points_topic: ""
