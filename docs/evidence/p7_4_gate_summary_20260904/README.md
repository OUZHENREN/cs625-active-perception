# P7.4 MoveIt Gate — PASS / FROZEN（2026-09-05）

所选视点 `geometry_nbv_e75_v-110_a22` 已完成实际 MoveIt 规划、Gazebo 执行和移动后真实 RGB-D / 位姿更新。命令以 `camera_depth_optical_frame` 表达，经实时 TF 转换至规划端 `my_end_effector_link`；r9 使用冻结关节分支并执行同一次规划返回的轨迹。

| 项目 | 实测结果 |
| --- | --- |
| 候选过滤 | P7.3 的 240 个候选中，25 个严格静态通过者逐一进入六级 MoveIt 过滤；该次单 IK 分支 3/25 规划通过、22/25 碰撞拒绝 |
| r9 规划 / 执行 | 0.078895 s / 13.000007 s；1 次请求，0 次重规划 |
| 关节路径 / 相机路径 | 5.212389 rad / 5.751655 m；后者为 20 Hz TF 折线，206 样本、0 次 TF 缺失 |
| 实际相机终点误差 | 0.688 mm / 0.080°；控制器结果码 0 |
| 移动后感知 | 640×480，明确预热 1 窗后连续 3/3 窗通过 |
| 移动后位姿 | 3/3 完整 SE(3)；平移 0.377 mm，旋转 1°，ADD-S 0.574 mm；yaw 标准差 2.887° |
| Gazebo 环境接触 | 记录覆盖命令至回执；该 1 条轨迹未观察到非预期机器人–地面/遮挡器/目标接触 |

22/25 是这一次采样 IK 分支的碰撞拒绝率，不能推论这 22 个视点本质不可达；a22 的另一冻结分支已独立验证并成功执行。Gazebo 接触记录与 MoveIt 状态碰撞拒绝独立统计；前者不覆盖自接触，不能报告全范围物理碰撞率为零。

三窗来自同一固定场景，不能充当三个独立随机 episode。验收范围仅为 severe-v5 番茄罐的 P7.4 接口及执行链，P7.5 抓取闭环仍未验收。

权威原始证据见 `../p7_4_batch_moveit_20260904_r4/` 与 `../p7_4_moveit_nbv_20260905_r9_integrated/`，相对路径及数值详见 [summary.json](summary.json)。r1/r3 规划失败、r5 位姿旋转超阈值、r6/r7 初始化错误、r9 首次 TF 预热不足均保留，未改名为通过。
